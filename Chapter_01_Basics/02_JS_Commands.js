console.log(process.platform);
// Platform specifies the operating system on which the Node.js process
// is running. It can be 'win32' for Windows, 'darwin' for macOS, and '
//linux' for Linux.
console.log(process.arch);
console.log("Node version is: " + process.version);